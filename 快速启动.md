# 快速启动

## 一键启动（推荐 · 无窗口）

**双击桌面快捷方式即可（已配置好，无黑框、无窗口）：**

| 桌面图标 | 用途 |
|---|---|
| `数据分析 Agent 平台` | 正式模式：后台静默启动 → 浏览器打开品牌启动页 → 自动跳转登录页 |
| `数据分析平台-演示模式` | 演示模式：免登录 + 自动加载示例数据 |
| `数据分析平台-停止服务` | 停止后台服务（无窗口） |

**完整体验流程**：双击图标 → 后端在后台静默启动 → 浏览器自动打开启动页（地球夜景动画 + Logo + 加载动画）→ 启动页实时探测后端就绪 → 就绪后自动跳转登录页 / 首页。

> 如果桌面没有快捷方式，用项目目录里的 `启动数据分析平台.lnk` / `启动数据分析平台-演示.lnk` / `停止服务.lnk`。
> 快捷方式指向 `pythonw launch_gui.pyw --silent [--demo]`——`pythonw` 保证无控制台窗口。

## 开发者方式（命令行）

在项目根目录打开 PowerShell：

```powershell
.\启动.ps1
```

它会自动：

1. 检查 Python 与依赖（缺了自动 `pip install -r requirements.txt`）
2. 检查 `.env` 里的 `LLM_API_KEY`（没配会提示但仍可启动，走关键词兜底）
3. 找空闲端口启动 FastAPI 后端（默认 8000，被占自动跳 8001）
4. 找空闲端口启动 HTML 前端（默认 8501，被占自动跳 8502）
5. 健康检查后端 `/health`
6. 自动打开浏览器访问前端

**停止后台服务**：

```powershell
.\启动.ps1 -Stop
```

## 常用参数

| 参数 | 作用 |
|---|---|
| `-NoBrowser` | 启动但不开浏览器（远程/无头环境用） |
| `-Stop` | 停止所有由本脚本启动的进程 |
| `-Check` | 仅做依赖/配置检查，不启动 |
| `-BackendPort 8001` | 指定后端端口 |
| `-FrontendPort 8502` | 指定前端端口 |

## 启用 LLM（可选但推荐）

编辑 `.env`：

```dotenv
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_API_KEY=sk-你的真实 key
LLM_MODEL=deepseek-chat
```

不配也能跑，自然语言意图识别走关键词匹配兜底（`意图来源: 规则`）。
配了 key 后调 `/reports/generate` 时 `意图来源` 应为 `"LLM"`。

## 仅跑单元测试（不联网）

```powershell
python -m pytest tests\test_agent_意图.py -v
```

应当看到 `26 passed`。

## 故障排除

| 问题 | 原因 | 解决 |
|---|---|---|
| `执行策略` 报错 | PowerShell 默认不允许跑脚本 | `Set-ExecutionPolicy -Scope Process Bypass` 后再跑 |
| 后端起不来 | 8000 端口被占 | `.\启动.ps1 -BackendPort 8001` |
| 前端起不来 | 8501 端口被占 | `.\启动.ps1 -FrontendPort 8502` |
| 浏览器报 `ERR_CONNECTION_REFUSED` | 后端重启后端口变了，标签页停在旧地址 | 关闭旧标签页，用启动器显示的地址重新打开 |
| 登录后报 401 / 数据页打不开 | localStorage 残留旧 token（旧实例失效） | 新版已支持自动登出跳转登录页；仍异常就清一次浏览器站点数据（Cookie/localStorage）后重新登录 |
| `意图来源` 一直 `规则` | `.env` 里 LLM_API_KEY 还是占位 | 改成真实 DeepSeek key |
| 停不掉进程 | PID 文件丢 | 任务管理器搜 `python.exe` 手动结束 |

## 启动后会得到的地址

| 用途 | URL |
|---|---|
| 前端页面（主入口） | http://127.0.0.1:8501 |
| 后端 API | http://127.0.0.1:8000 |
| API 文档（Swagger UI） | http://127.0.0.1:8000/docs |
| 健康检查 | http://127.0.0.1:8000/health |
