# 自助式数据分析Agent平台
## HTML + FastAPI + ECharts 轻量化交付说明

**交付日期**: 2026-07-22  
**当前定位**: 纯文件上传数据分析与可视化报表 Agent  

---

## 一、产品主链路

当前项目已经从 MySQL / NL2SQL 主链路调整为文件上传报表主链路：

```text
上传 CSV / Excel
    ↓
FastAPI 读取为 pandas.DataFrame
    ↓
字段画像与数据预览
    ↓
自然语言描述分析目标
    ↓
选择图表类型、X轴、Y轴、分组字段、聚合方式
    ↓
生成 ECharts 可视化报表、报表数据表和结论
```

---

## 二、推荐主入口：HTML 前端

### 1. 启动后端

```powershell
cd D:\python\agent\自助式数据分析Agent平台
python api\main.py
```

后端默认地址：

```text
http://127.0.0.1:8000
```

核心接口：

```text
GET  /health
POST /datasets/upload
GET  /datasets/{dataset_id}
POST /reports/generate
```

### 2. 启动 HTML 前端

```powershell
cd D:\python\agent\自助式数据分析Agent平台
uvicorn 前端_html.app:app --host 127.0.0.1 --port 8501
```

然后打开：

```text
http://127.0.0.1:8501
```

---

## 三、备用入口：Streamlit MVP

Streamlit 上传报表页面仍可作为调试入口：

```powershell
cd D:\python\agent\自助式数据分析Agent平台
streamlit run 前端_streamlit\主入口.py
```

---

## 四、MySQL 主链路状态

MySQL / NL2SQL 已从当前主链路中移除：

- 不再把 MySQL 作为用户数据入口
- 不再要求配置 `MYSQL_*`
- 不再依赖 `pymysql`
- 当前主路径是用户上传文件生成报表

---

## 五、核心文件

```text
api/main.py
api/contracts.py
api/routes/datasets.py
api/routes/reports.py
后端_核心/文件数据服务.py
后端_核心/数据画像.py
后端_核心/上传报表生成器.py
前端_html/app.py
前端_html/templates/index.html
前端_html/static/css/app.css
前端_html/static/js/api.js
前端_html/static/js/app.js
```

---

## 六、验证命令

```powershell
cd D:\python\agent\自助式数据分析Agent平台
python -m compileall api config 后端_核心 前端_html 前端_streamlit
node --check 前端_html\static\js\app.js
node --check 前端_html\static\js\api.js
```

核心报表函数 smoke test：

```powershell
@'
import pandas as pd
from 后端_核心.上传报表生成器 import 生成报表数据

df = pd.DataFrame({
    '月份': pd.date_range('2026-01-01', periods=4, freq='ME'),
    '地区': ['华东', '华南', '华东', '华北'],
    '销售额': [100, 150, 130, 180],
    '订单数': [10, 15, 12, 20],
})
for chart in ['自动推荐', '柱状图', '折线图', '饼图', '散点图', '表格']:
    report = 生成报表数据(df, 分析需求='测试需求', 图表类型=chart, x轴='月份', y轴=['销售额'], 分组字段='地区', 聚合方式='求和')
    print(chart, report['图表类型'], report['图表配置']['类型'], len(report['报表数据']))
'@ | python -
```
