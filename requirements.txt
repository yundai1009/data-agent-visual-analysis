# 版本已锁定（P1 加固）：防止依赖漂移导致 CI/生产行为不一致（如 pandas 3 日期推断变更）

# 后端
fastapi==0.141.1
uvicorn==0.34.0
python-multipart==0.0.20
python-dotenv==1.1.1
requests==2.32.3
pandas==2.2.3
openpyxl==3.1.5
reportlab==5.0.0

# Agent 记忆
chromadb==1.5.9

# 前端
streamlit==1.59.2
streamlit-option-menu==0.4.0
streamlit-ace==0.1.1
plotly==6.3.1

# 测试
pytest==8.4.2
pytest-timeout==2.4.0
jieba==0.42.1
playwright==1.61.0
# 旧版 .xls 读取（B2 修复：白名单支持 .xls，缺少该依赖会 ImportError）
xlrd==2.0.2

# 静态加密（LLM Key 落库加密）
cryptography==44.0.3
