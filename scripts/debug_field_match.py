"""debug: 规则字段匹配 — 验证"工作时间占比"的字段选择。"""
import sys
sys.path.insert(0, "D:/python/数据分析agent/自助式数据分析Agent平台")
from 后端_核心.上传报表生成器 import _受控语句配置, _意图驱动配置, _匹配意图字段, 字段意图关键词

画像 = {
    "字段列表": ["地点", "工作时间", "职位ID"],
    "分类字段": ["地点"],
    "数值字段": ["工作时间", "职位ID"],
    "日期字段": [],
    "文本字段": [],
}

需求 = "工作时间占比"
print("1. _匹配意图字段:", _匹配意图字段(画像, 字段意图关键词))
print("2. _受控语句配置:", _受控语句配置(画像, 需求))
print("3. _意图驱动配置:", _意图驱动配置(画像, 需求))
