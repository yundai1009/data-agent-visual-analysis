# -*- coding: utf-8 -*-
# ═══════════════════════════════════════════════════════════
# 【兼容 shim】此文件仅为「重构前中文文件名 → 重命名后英文模块」的过渡转发，
# 便于 eval_agent.py / 旧测试 / 外部脚本在未更新 import 时仍可运行。
#
# ⚠️ 注意：`import *` 只导出**公开函数/常量**（非下划线开头）。
#   若外部代码需要引用下划线私有函数（如 _意图驱动配置 / _执行一轮），
#   请直接 import 真实英文模块，例如：
#       from 后端_核心.field_selector import _意图驱动配置
#       from 后端_核心.agent.orchestrator import _执行一轮
#
# 确认全部调用方已迁移后可安全删除本文件。
# ═══════════════════════════════════════════════════════════

from .field_selector import *  # noqa: F401,F403
